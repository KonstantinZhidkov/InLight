import React from 'react';
import {preloader} from "../../images/images";

const Preloader = () => {
    return (
      <div>
          <img src={preloader} />
      </div>
    );
}

export default Preloader;